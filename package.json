{
  "name": "cccollab",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root: the @flatoutsolutions/cccollab MCP server workspace and the shared Convex backend.",
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "mcp_server"
  ],
  "type": "module",
  "scripts": {
    "dev": "npx convex dev",
    "build": "yarn workspaces foreach -A --topological-dev --exclude cccollab run build",
    "test": "yarn workspaces foreach -A --exclude cccollab run test",
    "test:integration": "yarn workspace @flatoutsolutions/cccollab run test:integration",
    "lint": "yarn workspaces foreach -A --exclude cccollab run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "yarn workspaces foreach -A --exclude cccollab run typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "convex": "^1.35.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  },
  "resolutions": {
    "hono": "^4.12.14"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
